클래스컴포넌트는 라이프 사이클이 존재한다.

- componentDidMount() : 컴포넌트가 평가되고 DOM에 렌더링 될 때. useEffect(..., [])
- componentDidUpdate() : 컴포넌트가 갱신되면 실행. useEffect(..., [someValue])
- componentWillUnmount() : 컴포넌트가 DOM에 삭제되기 직전에 실행. useEffect(()=> return ()=>{...}, [])

